import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mx.edu.uttt.DestiCode',
  appName: 'DestiCode',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
